apiVersion: v1
kind: Service
metadata:
  name: abekrenev-devops-final-task-service
spec:
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: abekrenev-devops-final-task
  type: NodePort
